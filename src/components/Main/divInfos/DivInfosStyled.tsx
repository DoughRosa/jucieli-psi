import styled from "styled-components";

const DivInfosStyled = styled.div`
    background-color: white; 
    color: ${(props) => props.theme.colors.quartiary};
    display: flex;
`

export default DivInfosStyled;